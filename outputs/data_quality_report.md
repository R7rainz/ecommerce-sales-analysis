# Data Quality Report

Generated by `src/01_data_cleaning.py`.

## Summary

- Rows read from source: **15,000**
- Rows in cleaned dataset: **15,000**
- Rows removed: **0** (0.00%)

## Checks Performed

| Check | Result | Action |
| --- | --- | --- |
| Schema | all 12 expected columns present | no change needed |
| Text whitespace | 0 value(s) had leading/trailing or repeated whitespace | no change needed |
| Missing values | none found in any column | no change needed |
| Duplicate rows | 0 fully identical row(s); 0 repeated Order_ID(s) with differing values | no change needed |
| Date format | 0 unparseable date(s); range 2023-01-01 to 2024-12-31 | parsed as datetime, no change needed |
| Numeric formats | 0 non-numeric value(s) across Quantity, Sales_Amount, Discount, Profit | cast to int/float |
| Value ranges | 0 non-positive Quantity, 0 non-positive Sales_Amount, 0 Discount outside 0.0-1.0 | all values within range, no change needed |
| Negative profit | 0 order(s) sold at a loss | kept - a loss is a valid business outcome, not a data error |
| Product -> Category mapping | 0 product(s) appear under more than one category | consistent, no change needed |
| State -> City mapping | every one of the 7 states is paired with all 7 cities - the State/City relationship is randomised and not geographically real | NOT corrected - flagged as a known limitation; treat State and City as independent labels, never as a hierarchy |
| Derived columns | added Order_Month, Order_Year, Profit_Margin | supports monthly trend and margin analysis |

## Interpretation

The source file arrived in unusually good condition: no missing cells,
no duplicate orders, consistent category labels, and no out-of-range
numbers. The cleaning pipeline still runs every check so the result is
verified rather than assumed, and so it stays correct if the source is
refreshed with messier data.

One genuine defect did surface. Every state in the file is paired with
every city, so combinations such as *Uttar Pradesh / Chennai* occur.
The geography was generated at random. State-level and city-level
figures are each internally consistent and safe to report, but the two
must never be combined into a drill-down, and no conclusion should be
drawn about which city drives a given state.
